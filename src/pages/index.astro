---
import Layout from "../layouts/Layout.astro";
import { Image } from "astro:assets";
import heroImage from "../assets/hero-image.webp";
import Concepto from "../components/Concepto.astro";
import Impacto from "../components/Impacto.astro";
import Beneficios from "../components/Beneficios.astro";
import Contacto from "../components/Contacto.astro";
import Separador from "../components/Separador.astro";
---

<Layout
  title="Tu Primer Hogar | Alquiler con derecho a compra"
  description="Accede a tu vivienda sin pagar entrada inicial. Nuestro modelo convierte tu alquiler mensual en inversión para tu futuro hogar."
  navbarTransparent={true}
>
  <!-- SECCIÓN HERO -->
  <section class="relative h-screen min-h-[600px] flex items-center justify-center overflow-hidden">
    
    <!-- 2. IMAGEN LCP CON MÁXIMA OPTIMIZACIÓN -->
    <Image
      src={heroImage}
      alt="Pareja disfrutando de su nuevo hogar en Tu Primer Hogar"
      class="absolute inset-0 w-full h-full object-cover z-0"
      loading="eager"      /* Carga inmediata */
      fetchpriority="high" /* Prioridad máxima para el navegador */
      width={1920}         /* Definimos dimensiones para evitar CLS */
      height={1080}
    />

    <div class="absolute inset-0 bg-black/50 z-0"></div>

    <div class="relative z-10 text-center text-white px-4 max-w-5xl mx-auto mt-16">
      <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold mb-6 leading-tight uppercase tracking-tight drop-shadow-md">
        Tu hogar<br />
        <span class="text-blue-400">sin pagar entrada</span>
      </h1>

      <p class="text-lg md:text-2xl mb-12 text-gray-100 max-w-2xl mx-auto drop-shadow font-light">
        Aprovecha tu alquiler mensual para la compra de tu futura vivienda. Sin letra pequeña.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center w-full">
        <a href="/contacto" class="w-full sm:w-auto bg-blue-600 hover:bg-blue-700 text-white font-bold py-4 px-10 rounded-full text-lg transition-all transform hover:-translate-y-1 shadow-lg shadow-blue-500/50">
          Solicita más información
        </a>
        <a href="#modelo" class="w-full sm:w-auto bg-transparent hover:bg-white/10 text-white font-bold py-4 px-10 rounded-full text-lg transition-all border-2 border-white backdrop-blur-sm">
          ¿Cómo funciona?
        </a>
      </div>
    </div>

    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce text-white/70 z-10">
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
      </svg>
    </div>
  </section>
  
  <Concepto />
  <Separador />
  <Impacto />
  <Separador />
  <Beneficios />
  <Contacto />
</Layout>