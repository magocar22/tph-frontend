---
// src/components/Navbar.astro
import logoBlanco from '../assets/tph-logo-blanco.png';

interface Props {
  isTransparent?: boolean;
}

const { isTransparent = false } = Astro.props;

const links = [
  { name: 'Inicio', url: '/' },
  { name: 'Modelo', url: '/modelo' },
  { name: 'Proyectos', url: '/proyectos' },
  { name: 'Inversión', url: '/invertir' },
  { name: 'Accionistas', url: '/accionistas' },
  { name: 'Prensa', url: '/blog' },
];

// 📍 LÓGICA DE ESTADO ACTIVO (Saber en qué página estamos)
const currentPath = Astro.url.pathname;

// Función para normalizar URLs (quitar barras finales para comparar bien)
const normalize = (path: string) => path.replace(/\/$/, '') || '/';

const isActiveLink = (url: string) => {
  const normCurrent = normalize(currentPath);
  const normUrl = normalize(url);
  
  // Si es la Home, coincidencia exacta. Si son otras secciones, "empieza por" (para subpáginas)
  if (normUrl === '/') return normCurrent === '/';
  return normCurrent.startsWith(normUrl);
};
---

<nav 
  id="main-navbar" 
  class:list={[
    "fixed top-0 w-full z-50 transition-all duration-300",
    isTransparent 
      ? "bg-transparent py-6" 
      : "bg-white py-4 shadow-md"
  ]}
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center">
      
      <!-- LOGO -->
      <a href="/" class="flex items-center relative z-50" aria-label="Volver al inicio">
        <div id="logo-container" class:list={[
                "transition-all duration-300",
                !isTransparent ? "logo-blue" : "" 
            ]}>
            <div 
                id="logo-mask"
                class="w-48 md:w-64 h-14 md:h-20"
                style={`--logo-url: url('${logoBlanco.src}')`}
            ></div>
        </div>
      </a>

      <!-- MENÚ ESCRITORIO -->
      <div class="hidden xl:flex items-center space-x-8">
        {links.map(link => {
          const active = isActiveLink(link.url);
          return (
            <a 
              href={link.url} 
              data-active={active ? "true" : "false"}
              class:list={[
                "nav-link text-sm font-medium transition-all duration-300 border-b-2",
                // LÓGICA DE COLORES INICIAL
                isTransparent 
                  ? (active ? "text-white border-white font-bold" : "text-white border-transparent opacity-90 hover:opacity-100") 
                  : (active ? "text-blue-600 border-blue-600 font-bold" : "text-gray-600 border-transparent hover:text-blue-600")
              ]}
            >
              {link.name}
            </a>
          )
        })}

        {/* 
            🔴 [DESACTIVAR ÁREA PRIVADA] - PASO 1 (HTML ESCRITORIO)
        */}
        <a 
            id="btn-inversores"
            href="/inversores" 
            class:list={[
                "text-sm font-medium px-4 py-2 rounded-full border transition-all flex items-center gap-2",
                isTransparent 
                    ? "border-white text-white hover:bg-white hover:text-blue-900" 
                    : "border-blue-600 text-blue-600 hover:bg-blue-50"
            ]}
        >
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path></svg>
            Área Privada
        </a>
        {/* 🔴 FIN PASO 1 */}

        <a 
            href="/contacto" 
            class="bg-blue-600 text-white text-sm font-bold px-5 py-2.5 rounded-full hover:bg-blue-700 transition-transform hover:-translate-y-0.5 shadow-lg"
        >
            Contactar
        </a>
      </div>

      <!-- BOTÓN HAMBURGUESA -->
      <div class="xl:hidden flex items-center relative z-50">
        <button id="mobile-menu-btn" aria-label="Abrir menú de navegación" class:list={["focus:outline-none transition-colors p-2", isTransparent ? "text-white" : "text-gray-800"]}>
          <svg id="icon-menu" class="w-8 h-8 transform transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
          <svg id="icon-close" class="hidden w-8 h-8 transform transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>

    </div>
  </div>

  <!-- MENÚ MÓVIL -->
  <div id="mobile-menu" class="hidden xl:hidden fixed inset-0 bg-blue-600 z-40 transition-opacity duration-300 opacity-0 pointer-events-none">
    <div class="flex flex-col items-center justify-center h-full space-y-8 p-6">
      {links.map(link => {
        const active = isActiveLink(link.url);
        return (
          <a href={link.url} class:list={[
            "font-bold text-2xl transition-colors border-b-2",
            active ? "text-white border-white" : "text-blue-200 border-transparent hover:text-white"
          ]}>
            {link.name}
          </a>
        )
      })}
      
      <hr class="w-16 border-blue-400 opacity-50" />
      
      {/* 🔴 [DESACTIVAR ÁREA PRIVADA] - PASO 2 (HTML MÓVIL) */}
      <a href="/inversores" class="text-blue-100 font-medium text-lg flex items-center hover:text-white transition-colors">
        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path></svg>
        Área Privada
      </a>
      {/* 🔴 FIN PASO 2 */}

      <a href="/contacto" class="w-full max-w-xs bg-white text-blue-600 text-center font-bold text-lg py-4 rounded-2xl hover:bg-blue-50 shadow-xl transform active:scale-95 transition-all">
        Contactar ahora
      </a>
    </div>
  </div>

</nav>

<style>
  #logo-mask {
    background-color: white;
    mask-image: var(--logo-url);
    -webkit-mask-image: var(--logo-url);
    mask-size: contain;
    -webkit-mask-size: contain;
    mask-repeat: no-repeat;
    -webkit-mask-repeat: no-repeat;
    mask-position: center;
    -webkit-mask-position: center;
    transition: background-color 0.3s ease;
  }
  
  /* 🔵 AQUÍ ESTÁ EL CAMBIO DE COLOR DEL LOGO */
  .logo-blue #logo-mask {
    /* Usamos el azul corporativo exacto #0025A7 */
    background-color: #0025A7 !important;
  }
  
  .menu-open {
    opacity: 1 !important;
    pointer-events: auto !important;
  }
</style>

<script is:inline define:vars={{ isTransparent }}>
  // ELEMENTOS DEL DOM
  const navbar = document.getElementById('main-navbar');
  const logoContainer = document.getElementById('logo-container');
  const navLinks = document.querySelectorAll('.nav-link');
  const mobileBtn = document.getElementById('mobile-menu-btn');
  const iconMenu = document.getElementById('icon-menu');
  const iconClose = document.getElementById('icon-close');
  const menuMobile = document.getElementById('mobile-menu');
  
  // 🔴 [DESACTIVAR ÁREA PRIVADA] - PASO 3 (JAVASCRIPT)
  const btnInversores = document.getElementById('btn-inversores');

  // Lógica de Scroll
  window.addEventListener('scroll', () => {
    if (menuMobile.classList.contains('menu-open')) return;
    if (!isTransparent) return;

    if (window.scrollY > 50) {
      applyScrolledStyles();
    } else {
      applyTransparentStyles();
    }
  });

  function applyScrolledStyles() {
      navbar.classList.add('bg-white', 'shadow-md', 'py-4');
      navbar.classList.remove('bg-transparent', 'py-6');
      logoContainer.classList.add('logo-blue'); 
      
      // AJUSTE DE COLOR EN ENLACES AL HACER SCROLL
      navLinks.forEach(l => { 
        const isActive = l.getAttribute('data-active') === 'true';
        
        // Quitamos estilos transparentes
        l.classList.remove('text-white', 'border-white');
        
        // Aplicamos estilos sólidos (fondo blanco)
        if (isActive) {
          // Página activa: Texto azul corporativo y borde azul
          l.classList.add('text-blue-600', 'border-blue-600', 'font-bold');
        } else {
          // Página inactiva: Texto gris, hover azul
          l.classList.add('text-gray-600', 'border-transparent');
        }
      });

      mobileBtn.classList.remove('text-white');
      mobileBtn.classList.add('text-gray-800');

      // 🔴 [DESACTIVAR ÁREA PRIVADA] - PASO 4
      if (btnInversores) {
        btnInversores.classList.remove('border-white', 'text-white', 'hover:bg-white', 'hover:text-blue-900');
        btnInversores.classList.add('border-blue-600', 'text-blue-600', 'hover:bg-blue-50');
      }
  }

  function applyTransparentStyles() {
      navbar.classList.add('bg-transparent', 'py-6');
      navbar.classList.remove('bg-white', 'shadow-md', 'py-4');
      logoContainer.classList.remove('logo-blue');
      
      // AJUSTE DE COLOR EN ENLACES (TRANSPARENTE)
      navLinks.forEach(l => { 
        const isActive = l.getAttribute('data-active') === 'true';

        // Quitamos estilos sólidos
        l.classList.remove('text-gray-600', 'text-blue-600', 'border-blue-600', 'border-transparent');
        
        // Aplicamos estilos transparentes
        if (isActive) {
            // Página activa: Blanco puro y subrayado blanco
            l.classList.add('text-white', 'border-white', 'font-bold');
        } else {
            // Página inactiva: Blanco normal
            l.classList.add('text-white', 'border-transparent');
        }
      });

      mobileBtn.classList.add('text-white');
      mobileBtn.classList.remove('text-gray-800');

      // 🔴 [DESACTIVAR ÁREA PRIVADA] - PASO 5
      if (btnInversores) {
        btnInversores.classList.add('border-white', 'text-white', 'hover:bg-white', 'hover:text-blue-900');
        btnInversores.classList.remove('border-blue-600', 'text-blue-600', 'hover:bg-blue-50');
      }
  }

  // Lógica Menú Móvil
  mobileBtn.addEventListener('click', () => {
    const isOpen = menuMobile.classList.contains('menu-open');
    if (!isOpen) {
        // Abrir
        menuMobile.classList.remove('hidden');
        setTimeout(() => menuMobile.classList.add('menu-open'), 10);
        iconMenu.classList.add('hidden');
        iconClose.classList.remove('hidden');
        logoContainer.classList.remove('logo-blue'); 
        mobileBtn.classList.remove('text-gray-800');
        mobileBtn.classList.add('text-white');
        document.body.style.overflow = 'hidden';
    } else {
        // Cerrar
        menuMobile.classList.remove('menu-open');
        setTimeout(() => menuMobile.classList.add('hidden'), 300);
        iconMenu.classList.remove('hidden');
        iconClose.classList.add('hidden');
        document.body.style.overflow = '';
        
        // Restaurar estado según scroll
        if (window.scrollY > 50 && isTransparent) {
            logoContainer.classList.add('logo-blue');
            mobileBtn.classList.add('text-gray-800');
            mobileBtn.classList.remove('text-white');
        } else if (!isTransparent) {
            logoContainer.classList.add('logo-blue');
            mobileBtn.classList.add('text-gray-800');
            mobileBtn.classList.remove('text-white');
        }
    }
  });
</script>