---
// src/components/VisionMision.astro
import { Image } from 'astro:assets';
// Imagen de fondo para la sección
import imgFondo from '../assets/fondo-salon.webp';
---

<!-- 
  SECCIÓN CON FONDO PARALLAX 
  bg-fixed: Hace que la imagen se quede "quieta" mientras haces scroll.
-->
<section class="relative py-20 md:py-32 bg-fixed bg-center bg-cover" style={`background-image: url('${imgFondo.src}');`}>
  
  <!-- CAPA OSCURA DE SUPERPOSICIÓN -->
  <!-- Oscurecemos un poco la foto de fondo para que las tarjetas blancas resalten más -->
  <div class="absolute inset-0 bg-black/40"></div>

  <div class="relative z-10 max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 lg:gap-16">

      <!-- TARJETA 1: VISIÓN -->
      <!-- 
        backdrop-blur-md: Efecto de vidrio borroso detrás.
        bg-white/90: Fondo blanco al 90% de opacidad (se lee perfecto, pero se siente ligero).
      -->
      <div class="group bg-white/95 backdrop-blur-md p-10 rounded-3xl shadow-2xl border border-white/20 transition-transform duration-300 hover:-translate-y-2">
        
        <!-- ICONO (SVG incrustado para no depender de librerías) -->
        <div class="w-16 h-16 bg-blue-100 rounded-2xl flex items-center justify-center mb-6 text-blue-600 group-hover:scale-110 transition-transform">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7Z" />
            <circle cx="12" cy="12" r="3" />
          </svg>
        </div>

        <h3 class="text-3xl font-bold text-blue-600 mb-4">
          Nuestra Visión
        </h3>
        
        <p class="text-gray-700 text-lg leading-relaxed">
          Ser la plataforma de referencia que democratiza el acceso a la vivienda, eliminando las barreras de entrada financiera y creando un puente seguro entre el alquiler y la propiedad.
        </p>
      </div>

      <!-- TARJETA 2: MISIÓN -->
      <div class="group bg-white/95 backdrop-blur-md p-10 rounded-3xl shadow-2xl border border-white/20 transition-transform duration-300 hover:-translate-y-2">
        
        <!-- ICONO -->
        <div class="w-16 h-16 bg-blue-100 rounded-2xl flex items-center justify-center mb-6 text-blue-600 group-hover:scale-110 transition-transform">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M3 3v18h18" /><path d="M18.7 8l-5.1 5.2-2.8-2.7L7 14.3" />
          </svg>
        </div>

        <h3 class="text-3xl font-bold text-blue-600 mb-4">
          Nuestra Misión
        </h3>
        
        <p class="text-gray-700 text-lg leading-relaxed">
          Brindar una alternativa sostenible y flexible mediante un modelo de alquiler con asistencia a la compra, asegurando precios justos, transparencia y beneficios reales para inquilinos comprometidos.
        </p>
      </div>

    </div>
  </div>
</section>